<br/>
<br/>
<br/>
My tours

<div class="container">
	<%= link_to "Tao chuyen di moi", new_tour_path, class: "btn btn-primary" %>
	<br/>
	<% @tours.each do |tour| %>
	<div class="row">
		<div class="col-md-10">
			<%= link_to  "#{tour.title}", tour_path(tour.id) %>
		</div>
		<div class="col-md-2" >
			<%= link_to "Duyet thanh vien", tour_pick_member_path(tour.id), class: "btn btn-secondary" %>
		</div>
	</div>
	<% end %>

</div>
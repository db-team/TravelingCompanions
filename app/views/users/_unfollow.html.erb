<%= form_for(current_user.active_relationships.find_by(follower_id: current_user.id, followed_id: @user.id), html: { method: :delete}, remote: true) do |f| %>
	<%= f.submit "Unfollow", class: 'btn btn-danger' %>
<% end %>
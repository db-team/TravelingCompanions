<nav class="navbar navbar-fixed-top navbar-light index">
  <div class="container">
    <a class="navbar-brand" id="logo" href="/">TravelGo</a>
  <ul class="nav navbar-nav">
    <li class="nav-item <%= 'active' if current_page?(root_path) %>">
      <%= link_to '<i class="fa fa-home" aria-hidden="true"></i> Home'.html_safe, root_url, :method => :get, class: 'nav-link' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?(tours_path) %>">
      <%= link_to '<i class="fa fa-motorcycle" aria-hidden="true"></i> Trips'.html_safe, tours_path, :method => :get, class: 'nav-link' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?(blogs_path) %>">
      <%= link_to '<i class="fa fa-book" aria-hidden="true"></i> Blogs'.html_safe, blogs_path, :method => :get, class: 'nav-link' %>
    </li>
  </ul>
  <ul class="nav navbar-nav pull-xs-right">
    <% if logged_in? %>
      <li class="nav-item dropdown">
        <a href="#" class="nav-link dropdown-toggle" type="" id="dropdownUserAction" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" >
          <i class="fa fa-plus" aria-hidden="true"></i>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownUserAction">
          <%= link_to "Create new trip", new_tour_path, class: 'dropdown-item' %>
          <%= link_to "Create your own blog", new_blog_path, class: 'dropdown-item' %>
        </div>
      </li>
      <li class="nav-item dropdown non_arrow">
        <a href="#" class="nav-link dropdown-toggle" type="" id="dropdownUserImage" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" >
          <%= image_tag(current_user.avatar_url, size: "40x40", :class => 'img-circle') %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownUserImage">
          <%= link_to "#{current_user.name}<br><b>@#{current_user.username}</b>".html_safe, user_path(current_user), class: 'dropdown-item' %>
          <div class="dropdown-divider"></div>
          <%= link_to '<i class="fa fa-cog" aria-hidden="true"></i> Setting'.html_safe, edit_user_path(current_user), class: 'dropdown-item' %>
          <%= link_to '<i class="fa fa-sign-out" aria-hidden="true"></i> Log ount'.html_safe, logout_path, method: "delete", class: 'dropdown-item' %>
        </div>
      </li>
    <% else %>
      <%= link_to '<i class="fa fa-user-plus" aria-hidden="true"></i> Sign up'.html_safe, new_user_path, class: 'btn btn-secondary' %>
      <%= link_to '<i class="fa fa-sign-in" aria-hidden="true"></i> Login'.html_safe, login_path, class: 'btn btn-success' %>
    <% end %>
  </ul>
  <!--
  <form class="form-inline pull-xs-right">
    <input class="form-control" type="text" placeholder="Search">
    <button class="btn btn-outline-success" type="submit">Search</button>
  </form>
  -->
  </div>
  
</nav>